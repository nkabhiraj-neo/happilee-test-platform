[build]
  publish = "qa-dashboard"
  command = "echo 'static site'"

[build.environment]
  GIT_SUBMODULE_STRATEGY = "none"

[functions]
  directory = "netlify/functions"

[[headers]]
  for = "/reports/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Cache-Control = "no-cache"
